# 实施方案：基于主被动结合的中小企业网络安全检测平台

> 2026-09-08 定稿。用户要求：分模块进行；**每个模块完成后报告**；**每模块有测试**；补齐不足；提供建议；不明处提问。
> 排期：按 6 个月宽松排期（至 2027-02~03）。工程规范：**Git 版本管理 + venv 虚拟环境**（用户已确认）。
> 设计依据：三份 v1.2 文档（毕设项目方案 / 需求方案 / 验收测试方案）。

## 1. 工作方式总则

1. **模块化推进**：每个里程碑拆成 2~6 个独立可交付模块；每模块工作量 2~7 天
2. **完成定义（DoD）**：功能实现 + 引擎层单测通过 + 对应 TC 用例执行（手动）+ 模块报告
3. **每模块完成后立即向用户报告**（报告模板见 §4），用户确认后再进下一模块
4. **测试分层**：L1 单测（unittest，引擎层每模块）→ L2 集成（冒烟/页面走查）→ L3 验收（TC 用例，按验收测试方案.md 对应编号）
5. **工程规范落地**：venv 隔离 + Git 每模块一个 commit（约定式提交信息：`M1-1: log_parser 实现 + 单测`）+ requirements.txt 固定版本
6. **文档为权威**：实施细节以三份 v1.2 文档为准；本方案只定模块边界、顺序与验收

## 2. 模块拆分总表（M0~M6 + AI = 32 模块）

| 模块 | 内容（产出文件） | 测试/验收 | 工作量 |
|---|---|---|---|
| **M0 环境与骨架** | venv 建环境、装依赖、git init + .gitignore、目录补全、config.py、应用工厂、models.py（14 表）、种子数据、启动冒烟 | app.py 可启动、/login 可达、建表成功 | 2~3 天 |
| M1-1 日志解析器 | engine/log_parser.py | test_log_parser 全绿 | 2~3 天 |
| M1-2 日志模拟器 | engine/log_simulator.py | test_simulator 全绿（契约自洽） | 2~3 天 |
| M1-3 实时流与工具 | utils/live_feed.py、utils/timewin.py | live_feed 单测 | 1 天 |
| M1-4 认证模块 | blueprints/auth.py + templates/login.html + base.html + 静态资源下载 | TC-AUTH-01~07 | 2~3 天 |
| M1-5 日志功能页 | blueprints/logs.py + logs.html + logs_sources.html + dashboard 简化 | TC-LOG/QUERY/DASH 本范围 | 3~4 天 |
| M1-6 M1 验收 | smoke.sh + M1 验收清单 8 条逐项 | 全部通过 + 报告 | 1 天 |
| M2-1 规则引擎 | engine/patterns.py + rules/builtin_rules.py + rule_engine.py | test_rule_engine（15 规则样例） | 3~4 天 |
| M2-2 告警服务 | engine/alert_service.py + 引擎→告警集成 | 爆破剧本 5 分钟自动告警 + count 累加 | 2~3 天 |
| M2-3 规则管理页 | blueprints/rules.py + rules.html（含规则测试工具） | TC-RULE-01~08 | 3 天 |
| M2-4 告警管理页+仪表盘 v1 | blueprints/alerts.py + alerts.html + dashboard 图表 | TC-ALERT-01~06、TC-DASH | 3~4 天 |
| M2-5 M2 验收 | 回归 + TC-RULE/ALERT/DASH 全量 | 通过 + 报告 | 1 天 |
| M3-1 特征提取 | engine/ml/features.py（12 维） | test_features | 2~3 天 |
| M3-2 ML 模型服务 | isolation_forest.py + kmeans.py（训练/预测/指标/贡献） | test_models | 3~4 天 |
| M3-3 ML 页面 | blueprints/ml.py + ml.html（训练/阈值滑块/解释） | TC-ML-01~05 | 3 天 |
| M3-4 ML 告警接入 | ml_detections→alerts（source_type=ml） | TC-ML-04 | 1 天 |
| M3-5 数据集验证★ | ml/dataset_adapter.py + tests/experiments/run_dataset_eval.py | TC-ML-06 → dataset_eval.md | 3~4 天（含数据集下载） |
| M3-6 M3 验收 | 回归 + TC-ML 全量 + 实验报告 | 通过 + 报告 | 1 天 |
| M4-1 本地靶场 | target_lab/（SQLi×2/XSS/敏感文件/遍历漏洞小站） | 手工验证漏洞可触发 | 2~3 天 |
| M4-2 扫描核心 | scanner/core.py + http_client.py（任务/并发/超时） | 任务调度单测 + 冒烟 | 3 天 |
| M4-3 信息收集 | info_gather/ 四子模块 | 对靶场输出正确 | 3 天 |
| M4-4 检测器首批 | detectors/sqli.py + xss.py（含对抗变体） | test_sqli/test_xss（含变体） | 3~4 天 |
| M4-5 扫描页面 | blueprints/scanner.py + scanner.html（目标/任务/结果） | TC-SCAN-01~05 | 3 天 |
| M4-6 M4 验收 | 靶场全检出 + TC-SCAN | 通过 + 报告 | 1 天 |
| M5-1 检测器补全★ | 其余 4 检测器（含对抗变体） | 各自单测（含变体用例） | 4~5 天 |
| M5-2 报告生成 | scanner/report.py + 报告模板 | TC-SCAN-06 | 2~3 天 |
| M5-3 主被动闭环★ | patterns.py 统一重构 + 告警↔扫描关联展示 | TC-SCAN-07 + 闭环演示 | 3 天 |
| M5-4 仪表盘补全 | IP 地图/TOP/占比（ECharts） | TC-DASH-03/04 | 2~3 天 |
| M5-5 误报率实验★ | tests/experiments/run_fp_eval.py | fp_eval.md（1 万条 ≤1%） | 2 天 |
| M5-6 M5 验收 | 全链路回归 + 核心答辩演示脚本化 | 通过 + 报告 | 1 天 |
| AI-1-1 AI 客户端 | ai/client.py + prompts.py（key 从 settings/env） | mock 测试（无 key 降级） | 2 天 |
| AI-1-2 AI 研判+设置页 | alert_review.py + 告警页研判卡片 + blueprints/settings.py + settings.html | TC-AI-01~03、TC-SET | 3~4 天 |
| AI-2-1 AI 报告+日报 | report_writer.py + daily_report.py + 报告页按钮 | TC-AI-04/05 | 3 天 |
| AI-2-2 AI 质量评估★ | ai/eval.py + run_ai_eval | TC-AI-08 → ai_eval.md | 2~3 天（含 key 与人工评分） |
| M6-1 全量回归 | 全部单测 + smoke.sh + 全部 TC | 无 S1/S2 遗留 | 2~3 天 |
| M6-2 交付物 | README + 启动脚本（run.bat）+ 演示脚本 + 截图 | 冷启动到演示 ≤10 分钟 | 3 天 |
| M6-3 性能基准★ | run_perf_eval.py | perf.md 达标 | 1~2 天 |
| M6-4 论文素材 | 实验报告归档 + 架构/截图整理 | 素材齐备 | 2 天 |

★ = 毕设深度补强模块（数据集/AI 评估/对抗/闭环/误报率），对应 v1.2 新增内容。

## 3. 执行顺序

- 主线严格按 M0→M6（依赖：模拟器→规则→告警→页面；扫描→报告→闭环）
- M1-1（parser）先于 M1-2（simulator）：契约耦合，模拟器输出须被 parser 解析
- AI 插入式：AI-1 在 M2 后（约第 2.5~3 月），AI-2 在 M5 后（约第 5 月）
- 补强模块（★）预留 ~2~3 周缓冲；时间吃紧按裁剪预案（毕设项目方案.md §8）

## 4. 每模块完成报告模板

```
## 模块报告：<模块名>
- 状态：完成 / 部分完成（说明） / 阻塞（原因+问题）
- 实现内容：…（3~5 行，产出文件清单）
- 测试结果：单测 X/Y 通过（失败项与修复状态）；TC 用例 P/F 清单
- 验收对照：<DoD 条目> 逐条 ✅/❌
- 已知问题/待办：…
- 下一步：<下一模块名>，预计 X 天
```

每模块报告后暂停，等用户确认再进入下一模块。

## 5. 不足补齐清单

| # | 不足 | 补齐动作 | 时机 |
|---|---|---|---|
| 1 | 无版本管理 | git init + .gitignore（data/、.venv/、__pycache__、*.db）+ 每模块一个 commit | M0 |
| 2 | 依赖未固定版本 | requirements.txt 记录实际安装版本 | M0 |
| 3 | 全局环境污染风险 | venv 建 .venv | M0 |
| 4 | Windows 中文路径/编码坑 | UTF-8 头声明；pathlib；config 从项目根定位 | M0 |
| 5 | 无一键启动 | run.bat（激活 venv + 启动 + 开浏览器） | M6-2 |
| 6 | 端口占用/迁移 | config 端口可配（默认 5000）；数据目录随项目走 | M0 |
| 7 | M2~M6 无文件级设计 | 每模块开工前先出模块级设计（1 页） | 每模块 |
| 8 | AI key 缺失时演示减色 | 预生成 AI 结果种子 | AI-2 后 |
| 9 | 单测覆盖盲区 | smoke.sh 统一入口 | M1-6 建立 |
| 10 | 论文素材散落 | docs/截图/ 按里程碑归档；docs/experiments/ 实验报告 | 全程 |

## 6. 用户配合事项

1. 每模块报告后确认（节奏由用户掌控）
2. M0 后确认 venv + 依赖安装、首次 git commit
3. AI 阶段前注册 DeepSeek 平台并充值提供 API key（可后补）
4. M3-5 前确认数据集下载条件（NSL-KDD ~8MB；CICIDS2017 用按日采样子集）
5. 靶场端口默认 5050（避免与主应用 5000 冲突）

## 7. 验证方式（每模块通用）

1. 引擎类：`python -m unittest tests/test_<模块>.py -v` 全绿
2. 页面类：启动应用 → 浏览器走查 + TC 用例逐条记录
3. 集成/验收：smoke.sh（M1-6 起）+ 里程碑验收清单
4. 实验类：产出 docs/experiments/*.md 为验收凭证
5. 每模块结束 git commit（信息含模块号）
