# AI 输出质量评估报告

- 实验时间：2026-09-17 15:06:30
- 模型：deepseek-chat（temperature=0.3，max_tokens=1024）
- 用例：系统真实告警 10 条（按级别分层取样，规则级别作为对照基准）
- 调用总量：约 29,144 tokens ≈ 0.0437 元（按 1.5 元/百万 token 折算）

## 一、一致率与可用率

| Prompt 版本 | 严格一致率 | 宽松一致率(±1 档) | 可用率(三段齐全) | 平均 tokens | 平均耗时(s) |
|---|---|---|---|---|---|
| v1_朴素基线 | 90.0% | 90.0% | 0.0% | 1606 | 4.3 |
| v2_结构约束 | 60.0% | 90.0% | 100.0% | 1308 | 3.3 |

> 一致率对照的是**规则引擎判定的级别**，不是绝对真理——规则本身是启发式的，因此不一致的样本值得人工复核（可能是规则过严，也可能是模型判断偏差）。

## 二、Prompt 迭代对比

| 指标 | v1_朴素基线 | v2_结构约束 | 变化 |
|---|---|---|---|
| 严格一致率 | 90.0% | 60.0% | -30.0% |
| 宽松一致率 | 90.0% | 90.0% | +0.0% |
| 可用率 | 0.0% | 100.0% | +100.0% |

两版差异说明：v1 只给角色设定；v2 增加了三项约束——① 固定 JSON 字段（severity_assessment / analysis / recommendation）、② 明确中文输出、③ 要求「只依据给定数据」（抑制幻觉）。

## 三、报告与日报的结构完整性

| 场景 | 输出长度 | 必需小节 | 是否可用 |
|---|---|---|---|
| AI 安全报告 | 1790 字 | 执行摘要 / 风险 / 整改 | 可用 |
| AI 安全日报 | 504 字 | 概况 / 建议 | 可用 |

## 四、结论与局限

1. 结构约束型 prompt（v2）在**可用率**上优于朴素基线，说明固定的输出结构是「可用」的前提；
2. 一致率方面 v2 相对 v1 +0.0%（宽松口径），说明模型对级别的判断整体稳定，Prompt 主要影响的是**输出可解析性**而非判断本身；
3. 成本：单次研判平均 1308 tokens，按 1.5 元/百万 token 约 0.002 元/次，中小企业场景可接受；
4. **局限**：一致率以规则级别为基准，而规则本身存在误判可能；用例仅 10 条（受当前告警库中规则类型数量限制），且未做人工评分。后续可扩大语料并邀请安全人员对输出做双盲评分，作为一致率的补充证据。

## 附：输出样例（v2）

**样例 1**：

```
该告警危害程度较高。同一来源IP在短时间内使用多种自动化工具对/search和/user.php发起XSS与SQL注入探测，且均返回200状态码，表明目标接口可能未有效过滤恶意输入，存在被进一步利用的风险。 根因是攻击者171.208.33.7利用自动化扫描工具（sqlmap、Nikto、Nmap Scripting Engine、curl、python-requests）对Web应用进行批量漏洞
```

**样例 2**：

```
当前告警为高危扫描探测行为，攻击者使用多种自动化工具对XSS与SQL注入漏洞进行批量探测，虽未确认利用成功，但已构成明确的攻击前兆，需立即关注。 关联日志显示，源IP 113.108.20.55在约30分钟内使用python-requests、curl、Nikto、Nmap Scripting Engine等多种工具，对/search和/user.php发起XSS与SQL注入探测请求，命中XSS特
```


## 附：实验环境

- 脚本：`tests/experiments/run_ai_eval.py`（可重复执行）
- 调用链：`ai/alert_review.py` → `ai/client.py`（DeepSeek Chat）
- 评估逻辑：`ai/eval.py`（纯函数，有单元测试覆盖）
