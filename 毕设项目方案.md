# 毕设项目方案：基于主被动结合的中小企业网络安全检测平台（Web 应用）

> 本文档为毕设完整设计方案。**M1（骨架+日志链路）已细化到文件/接口/字段级**，后续里程碑（M2~M6）实施前会再出同级详细设计并追加到本文档。
>
> 确认后即可按"M1 详细设计 → 6.10 实施顺序"开始实施。

**相关文档（三份方案的关系）**：
| 文档 | 内容 | 用途 |
|---|---|---|
| 《毕设项目方案.md》（本文档） | 系统"怎么做"：技术架构、模块设计、数据库、AI 设计、里程碑 | 论文第 4~5 章基础 |
| 《需求方案.md》 | 系统"要做什么"：FR-1~9 功能需求、NFR 非功能需求、MoSCoW 优先级 | 论文第 3 章基础；技术设计的输入依据 |
| 《验收测试方案.md》 | "怎么证明做对了"：TC 用例、检测效果/性能/安全测试、验收标准 | 论文第 6 章基础；测试执行依据 |

---

## 1. 项目概述

**学生背景**：网络工程本科，Python 技术栈，编码主要靠 AI 辅助实现 → 方案必须高度模块化（每个功能模块独立生成、独立测试、独立演示）。时间预算 3~6 个月。

**选题**：以"被动日志异常分析"（轻量 SIEM）为主体，融合"主动 Web 漏洞扫描"为补充模块。

**题目（定稿）**：《基于主被动结合的中小企业网络安全检测平台》

**题目核心创新点**：
1. **主被动结合**：主动漏洞扫描与被动日志检测共用统一攻击特征库（`patterns.py`），形成"扫描发现漏洞 → 日志检测攻击 → 统一告警"的检测闭环——与真实安全产品（WAF+扫描器联动）同构，答辩有亮点

**系统重要亮点（不占题目，论文重点章节）**：
2. **智能研判**：大模型（DeepSeek）对告警自动研判（危害/根因/处置建议）、自动生成安全报告与日报；与规则引擎（抓已知）、机器学习（抓未知）构成三层协同检测体系

**应用场景（用户画像见《需求方案.md》1.1 节）**：面向 20~200 人、1~10 台服务器、**无专职安全人员**的中小企业——买不起商业安全平台、日志无人分析。本系统以"被动检测替人盯日志 + 主动扫描替人做体检 + AI 研判给处置建议"覆盖其核心需求。边界：大型企业（有专职安全团队与商业 SOC）不在目标范围内。

**安全合规**：被动链路数据可模拟生成，零合规风险；扫描器只扫本地靶场（`target_lab/`，一个故意带 SQLi/XSS 漏洞的 Flask 小站）。

**项目目录**：`D:\VideCoding\毕设\security-platform\`

---

## 2. 技术栈与关键设计决策

### 技术栈

- Flask + Jinja2 服务端渲染 + Bootstrap 5（本地引入，防演示断网）+ ECharts 5（本地引入）
- SQLite + SQLAlchemy（10 万级日志可支撑演示；论文可写"生产可切 PostgreSQL，ORM 已解耦"）
- scikit-learn + pandas（孤立森林 / K-means，M3 引入）
- requests（扫描器 HTTP 客户端）；线程池（concurrent.futures）做任务调度，不引 Celery
- 后台线程 + queue.Queue 跑日志模拟器/扫描任务；生产/答辩演示用 waitress
- 测试用标准库 unittest（不引 pytest）

### 关键设计决策（整个项目的骨架，勿偏离）

1. **服务端渲染为主**：每个页面一个 Flask 路由返回完整 HTML，图表数据由后端序列化为 ECharts option（`chart_utils.py`），前端只写少量原生 JS 轮询。不引 Vue 构建链——对 AI 辅助编码最友好，出错直接看 HTML 排查。
2. **引擎层与 Web 层分离**：`secplat/engine/`（被动链路）和 `secplat/scanner/`（主动链路）**零 Flask import**，全部是纯 Python 类/函数，每个模块配独立单测（`tests/`）——AI 辅助开发的验收闸门：跑不过测试即拒绝合并。
   - 注：主包名为 `secplat`（原设计 `platform/`，因与 Python 标准库 `platform` 模块重名冲突，M0 实施时改名）
3. **共享特征模式库** `engine/patterns.py`：SQLi/XSS/目录遍历/命令注入等攻击特征正则的唯一定义处，规则引擎与扫描检测器共用——"主被动结合"创新点的落地文件。
4. **数据自造**：日志模拟器（`log_simulator.py`）按剧本生成正常+攻击流量（SSH 爆破/端口扫描/Web 攻击三类攻击剧本 + 正常流量），保证"永远有数据可演示"。
5. **依赖收敛**：requirements.txt 固定少量包，禁自定义深奥语法；所有复杂逻辑放 engine/scanner，blueprint 层只做"查库+渲染"。

---

## 3. 功能模块总览

### 前端 10 个页面（导航栏统一，继承 base.html）

| # | 页面 | 核心功能 | 里程碑 |
|---|---|---|---|
| 1 | 登录页 | 单管理员（登录失败锁定 + session 过期） | M1 |
| 2 | 仪表盘 | 统计卡片、攻击时间线、来源 IP 地图、TOP 攻击源、级别占比饼图 | M1 卡片版 / M2+ 图表 |
| 3 | 实时日志流 | 2 秒轮询 + 历史回放筛选 | M1 |
| 4 | 日志源管理 | 文件导入 / 模拟器配置启停 | M1 |
| 5 | 告警管理 | 筛选分页、详情关联原始日志与扫描发现、状态流转 | M2 |
| 6 | 规则管理 | CRUD、启停、**规则测试工具**（粘贴日志测命中，答辩演示利器） | M2 |
| 7 | 机器学习 | 训练触发、ROC/PR 曲线、**阈值滑块实时调**、特征贡献解释 | M3 |
| 8 | 扫描目标与结果 | 目标管理、发起扫描、漏洞卡片按级别着色 | M4 |
| 9 | 漏洞报告 | HTML 报告生成/下载，Ctrl+P 打印为 PDF | M5 |
| 10 | 系统设置 | AI key 配置、日志保留天数、实时流轮询间隔（key 不回显明文） | AI-1 |

### 被动检测引擎（engine/，零 Flask 依赖）

- `log_parser.py`：SSH/Web/Nmap 三种日志格式归一化为统一 LogEvent
- `log_simulator.py`：按剧本生成正常+攻击流量
- `rule_engine.py`：三类规则——regex（单事件正则）/ aggregate（窗口聚合计数）/ composite（复合序列）；15 条内置规则（SSH 暴力破解、端口扫描、扫描器 UA 指纹、SQLi/XSS/目录遍历/命令注入特征、敏感路径、高频错误、双重编码等）
- `alert_service.py`：同源同规则滑动窗口去重合并（count 累加）
- ML（M3）：`features.py` 按 src_ip×60s 聚合 12 维特征（请求数、端口多样性、失败占比、特殊字符密度、熵等）；孤立森林为主算法（输出异常分+Top-K 特征贡献解释），K-means 辅助（轮廓系数选 k，与 IF 结果对比）
- **公开数据集验证（M3 补强）**：`ml/dataset_adapter.py` 加载 NSL-KDD / CICIDS2017 公开数据集（ARFF/CSV → 系统特征 DataFrame 格式），实验脚本完成"模拟数据 vs 公开数据集"双场景训练评估——论文实验的可信数据来源，证明算法效果不依赖自造数据

### 主动扫描链路（scanner/，零 Flask 依赖）

- 信息收集：CMS 指纹（20+ 特征）、响应头分析、robots 解析、目录探测（200 条字典）、可选链接爬取
- 6 个检测器（每个独立文件、统一签名 `detect(urls, ctx) -> list[Finding]`、零共享状态）：SQLi（报错/时间盲注/真值假值对比）、反射 XSS、敏感文件泄露、目录遍历、安全响应头缺失、CVE 版本匹配（内置小表）
- **对抗性增强（M4/M5 补强）**：每个检测器内置 2~3 条绕过变体用例（大小写混淆、双重 URL 编码、注释符 `/**/` 混淆等），单测断言变体同样被检出——体现对抗性设计深度，防止"只会检最原始的 payload"
- `report.py`：Jinja2 模板生成 HTML/Markdown 报告，每条漏洞带修复建议

---

## 4. 数据库设计（14 张表，M1 先用 3 张）

时间字段统一存 ISO 字符串（便于展示与比较）。主键均为自增 id。

| 表 | 字段 | 里程碑 |
|---|---|---|
| users | username(unique), password_hash(PBKDF2), role, created_at | M1 ✅ |
| log_sources | name, source_type(import/simulator), file_path, scenario, rate, duration, status(running/stopped/finished), total_parsed, created_at | M1 ✅ |
| log_events | ts, log_type(ssh/web/scan/other), src_ip, dst_ip, src_port, dst_port, proto, method, url, status_code, user_agent, username, detail(JSON), raw, source_id；索引 (src_ip)(ts)(log_type) | M1 ✅ |
| rules | name, category, severity, rule_type, pattern, match_field, threshold, time_window, group_field, enabled, description, is_builtin | M2 |
| alerts | title, rule_id, severity, src_ip, dst_ip, url, status, count, first_seen, last_seen, detail(JSON), source_type(rule/ml)；索引 (src_ip) | M2 |
| ml_models | algo, trained_at, params(JSON), metrics(JSON), data_range | M3 |
| ml_detections | ts, algo, src_ip, anomaly_score, label, feature_vector(JSON), top_features(JSON), alert_id | M3 |
| scan_targets | url, name, status, last_scan_at, created_at | M4 |
| scan_tasks | target_id, status, detector_ids(JSON), concurrency, started_at, finished_at, finding_summary(JSON) | M4 |
| scan_findings | task_id, target_id, vuln_type, severity, url, param, payload, evidence, description, fix_suggestion, detected_at | M4 |
| scan_info_results | task_id, kind, content(JSON) | M4 |
| reports | task_id, target_id, file_path, created_at, summary(JSON) | M5 |
| settings | key, value（含 deepseek_api_key、log_retention_days、live_poll_interval） | M1 部分 / AI-1 |
| ai_insights | target_type(alert/report/daily), target_id, ai_type, model, status(ok/failed), output(JSON/Text), prompt_tokens, completion_tokens, created_at；索引 (target_type, target_id) | AI 阶段 |

---

## 5. 日志格式契约（模拟器生成 ↔ 解析器解析的统一定义）

三种格式，全部为文本行，按 `source_type` 分派解析：

**① SSH（syslog 风格）**
```
Mar  1 08:14:22 server sshd[1234]: Failed password for invalid user admin from 192.168.1.50 port 50231 ssh2
Mar  1 08:15:01 server sshd[1235]: Accepted password for zhangsan from 10.0.0.8 port 52100 ssh2
```
→ 字段：ts, log_type=ssh, src_ip, username, dst_port=22, detail={event: failed|success}, raw

**② Web（Apache combined 风格）**
```
192.168.1.50 - - [01/Mar/2026:08:14:22 +0800] "GET /index.php?id=1%27+OR+%271%27%3D%271 HTTP/1.1" 200 1234 "http://example.com/" "Mozilla/5.0 (Windows NT 10.0)"
```
→ 字段：ts, log_type=web, src_ip, method, url(含 query), status_code, user_agent, referer→detail, raw

**③ Nmap（自定义 CSV，模拟器专用）**
```
2026-03-01T08:14:22,192.168.1.50,192.168.1.100,22,tcp,open
```
→ 字段：ts, log_type=scan, src_ip, dst_ip, dst_port, proto, detail={state}, raw

解析失败返回 None（该行跳过，不中断）。时间解析失败则取当前时间兜底。

---

## 6. M1 详细设计（骨架 + 日志链路）

> 已细化到文件、接口、字段级。审阅通过后按此实施。

### 6.1 交付范围

可运行的 Flask 应用：登录 → 日志模拟器生成日志入库 → 日志查询/筛选/实时流页面 → 日志源管理（导入文件/启停模拟器）。含 parser/simulator 单测 + 冒烟脚本。

### 6.2 依赖与配置

- `requirements.txt`：`flask`、`flask-sqlalchemy`、`requests`、`waitress`（scikit-learn/pandas 到 M3 再加）
- `config.py`：`SECRET_KEY`、`SQLALCHEMY_DATABASE_URI`（`sqlite:///data/app.db`）、目录常量 `DATA_DIR / RAW_LOG_DIR / REPORT_DIR / MODEL_DIR`（`pathlib`，相对项目根）
- `app.py`：`create_app()` + `db.init_app` + 蓝图注册 + 启动（host=127.0.0.1, port=5000, threaded=True）
- `secplat/__init__.py` 应用工厂：加载 config → 初始化 db → `db.create_all()` → 种子数据（首次启动创建 admin 用户 + 一个示例 normal 模拟器源）→ 注册蓝图

### 6.3 引擎接口（零 Flask 依赖）

**log_parser.py**
- `LogEvent` dataclass：`ts, log_type, src_ip, dst_ip, src_port, dst_port, proto, method, url, status_code, user_agent, username, detail: dict, raw`
- `parse_line(line: str, source_type: str) -> LogEvent | None`
- `parse_file(path: str, source_type: str) -> Iterator[LogEvent]`（行级容错，坏行跳过）
- 内部：三种格式正则编译为模块级常量，`_parse_ssh / _parse_web / _parse_nmap` 三个私有函数

**log_simulator.py**
- 四个剧本：`normal`（随机内网 IP 正常 Web 请求 200 为主/成功 SSH 登录）、`ssh_bruteforce`（攻击 IP 池连续失败登录，用户名字典 root/admin/ubuntu/test...，夹带一次成功，混 15% 正常流量）、`port_scan`（对目标 IP 扫 20+ 端口，含 SYN/FIN/空标志特征）、`web_attack`（SQLi/XSS/目录遍历/敏感路径 payload 的 HTTP 请求，混 15% 正常流量）
- `generate(scenario: str, rate: float, duration: int) -> Iterator[str]`：按速率产出原始日志行
- `run_simulator(source_id, scenario, rate, duration, stop_event)`：后台线程——生成→parse→**批量入库（500 条/批）**→推实时队列→更新 log_sources.total_parsed/status
- 只依赖 `random` + `datetime` + 内置 IP/用户名字典

### 6.4 实时流机制（secplat/utils/live_feed.py）

- 全局 `deque(maxlen=500)` + `threading.Lock`；`push(event)` / `snapshot_after(after_id)`（返回 (序号, 新事件列表)）
- logs 页面 2 秒轮询 `/logs/api/live?after_id=N` 增量拉取
- 不依赖 Flask，可单测

### 6.5 蓝图与路由清单

**auth.py**：`GET/POST /login`、`GET /logout`、`login_required` 装饰器（未登录跳转 /login）。密码用 werkzeug `generate_password_hash / check_password_hash`（默认 scrypt）。**安全增强**：session 有效期 8 小时；连续 5 次登录失败锁定 5 分钟（内存字典实现，含剩余时间提示）。

**logs.py**：

| 路由 | 功能 |
|---|---|
| GET /logs | 查询页：筛选（log_type/src_ip/keyword/日期区间）+ 分页 100 条/页 |
| GET /logs/sources | 日志源管理页 |
| POST /logs/sources | 新增日志源（type=import_file 或 simulator） |
| POST /logs/sources/<id>/start | 启动模拟器（后台线程） |
| POST /logs/sources/<id>/stop | 停止（置 stop_event） |
| POST /logs/sources/<id>/delete | 删除源（保留已入库事件） |
| POST /logs/sources/<id>/do_import | 执行文件导入（读取 → parse_file → 批量入库） |
| GET /logs/api/events | JSON 分页查询（前端表格 AJAX） |
| GET /logs/api/live | 实时增量 |

**dashboard.py（M1 简化版）**：`GET /` 重定向、`GET /dashboard` 统计卡片（总事件数/今日事件数/日志源数/活跃源数）。

### 6.6 模板与静态资源

- `base.html`：顶部 Bootstrap 导航栏（仪表盘/日志/日志源，告警·规则·ML·扫描·报告为占位禁用项）、flash 消息区、content block、登录态（用户名+退出）
- `login.html`：居中卡片式登录表单
- `logs.html`：筛选栏（select 日志类型 + src_ip/keyword + 日期区间 + 查询按钮）+ 事件表格 + 分页 + "实时流"开关（原生 JS setInterval 轮询）
- `logs_sources.html`：日志源表格（名称/类型/剧本/速率/状态/已解析数/操作按钮）+ 新增区（文件导入表单 + 模拟器表单）
- `static/`：`bootstrap.min.css`、`bootstrap.bundle.min.js`、`echarts.min.js` 从 staticfile CDN 下载到本地、`app.css` 统一样式

### 6.7 测试设计（tests/，标准库 unittest）

- `test_log_parser.py`：三种格式正常样例各 2~3 条断言字段（ts/src_ip/url/status_code 等）；畸形样例（空行、乱行、字段缺失）返回 None；parse_file 夹坏行不中断
- `test_simulator.py`：四剧本各生成 100 条→**全部可被 parser 解析**（格式契约自洽）；速率近似（rate=50, duration=2s → 总数 80~120）；ssh_bruteforce 失败事件占比 >80%；port_scan 去重端口 ≥20
- `smoke.sh`：跑全部测试 + 断言服务可启动（Windows 下 git-bash 可执行）

### 6.8 种子数据

首次启动：建表 + admin 用户（admin / admin123，README 注明改密）+ 一个示例日志源（模拟器剧本 normal）+ settings 默认值（deepseek_api_key 空、log_retention_days=30、live_poll_interval=2）。

**日志数据膨胀控制**：应用启动时按 `log_retention_days` 清理过期 log_events（保留策略可在系统设置页修改）。

### 6.9 M1 验收清单（端到端）

1. `python app.py` 启动无报错；访问 /login 正常
2. admin/admin123 登录 → 跳转 /dashboard，统计卡片显示数据
3. 日志源页新建"SSH 爆破"模拟器 → 启动 → **快速演示 rate=50, duration=10（10 秒 ≥500 条）**；长时运行（rate=20, duration=60）留论文实验
4. 日志页按 log_type=ssh / src_ip 筛选结果正确；分页正常
5. 实时流开关开启后 2 秒刷新显示新事件
6. 文件导入：准备一个 100 行样例日志文件 → 导入后入库 100 条
7. 连续 5 次错误密码 → 提示锁定；正确密码在锁定解除后可用
8. `python -m unittest` 全绿；smoke.sh 通过

### 6.10 实施顺序（M1 内，每步可独立验证）

1. requirements + config + 应用工厂 + models（含 create_all + admin 种子）→ 启动冒烟
2. log_parser + test_log_parser → 跑绿
3. log_simulator + test_simulator → 跑绿
4. live_feed + 时间工具
5. auth 蓝图 + base/login 模板 + 下载静态资源
6. logs 蓝图 + logs/logs_sources 模板 + dashboard 简化版
7. 端到端按 6.9 验收 + README

---

## 6A. AI 集成设计（DeepSeek 大模型，新增）

> 场景：AI 告警研判 + AI 报告生成 + AI 安全日报。接入 DeepSeek API。

### 6A.1 三个 AI 场景

| 场景 | 输入 | 输出 | 页面接入 | 前置依赖 |
|---|---|---|---|---|
| AI 告警研判 | 单条告警 + 关联日志样本（5~10 条）+ 该 IP 事件统计 | JSON：severity_assessment / analysis（危害与根因）/ recommendation（处置建议） | 告警详情页"AI 研判"按钮 → 研判卡片 | M2 告警 |
| AI 报告生成 | 扫描任务 findings 聚合（类型/级别/URL/修复建议汇总） | 自然语言安全报告（执行摘要、风险分级、重点漏洞解读、整改优先级） | 报告页"AI 生成报告" → 预览 | M5 扫描器 |
| AI 安全日报 | 最近 24h 告警统计 + 规则命中分布 | 自然语言日报（摘要、TOP 风险、趋势、建议） | 报告页"AI 安全日报" → 预览 | M2 告警（数据量） |

### 6A.2 架构（secplat/ai/，零 Flask 依赖）

```
secplat/ai/
├── __init__.py
├── client.py          # DeepSeek Chat 客户端
├── prompts.py         # system/user prompt 模板 + JSON 结构定义
├── alert_review.py    # review_alert(alert, sample_events, ip_stats) -> dict
├── report_writer.py   # write_security_report(findings_summary) -> str
├── daily_report.py    # write_daily_report(alerts_24h, rule_stats) -> str
└── eval.py            # ★ AI 质量评估实验（AI-2 补强，见 6A.5）
```

**client.py 要点**：
- `POST https://api.deepseek.com/chat/completions`，`Authorization: Bearer <key>`，model=`deepseek-chat`，timeout=30s，max_tokens=1024，temperature=0.3
- key 来源：settings 表 `deepseek_api_key` 或环境变量 `DEEPSEEK_API_KEY` 覆盖；仅服务端持有，前端永不出现
- **系统设置页**（AI 阶段新增路由 `GET/POST /settings`）：页面配置 deepseek_api_key、log_retention_days、live_poll_interval；key 输入后不回显明文（只显示已配置/未配置状态）
- 失败（无 key/网络/超时/4xx）抛 `AIError`，调用方捕获后降级
- 只用 requests 直接调用（OpenAI 兼容接口），不引 openai SDK（依赖收敛）

**prompts.py 要点**：
- system prompt：角色 = 资深安全分析师；输出约束 = 严格 JSON（字段名固定），中文输出
- 上下文组装**截断**：日志样本只取 5~10 条、每条截断到 ~300 字符、告警关联信息只取关键字段——成本控制
- 告警研判要求 JSON 三段：severity_assessment / analysis / recommendation；报告与日报要求纯文本 Markdown

### 6A.3 数据与降级

- **ai_insights 表**：见第 4 节；索引 (target_type, target_id)
- **降级策略（关键设计决策）**：无 key / 断网 / 超时 / 解析失败 → 页面提示"AI 服务不可用，可查看历史结果"，核心检测链路完全不受影响
- **历史回放**：已生成的研判/报告存 ai_insights，可重复查看（答辩演示不依赖网络的关键保障）
- **注入防护**：LLM 输出仅经 Jinja2 转义渲染，不执行、不进 SQL、不 eval
- **成本**：DeepSeek 约 ¥1~2/百万 input token，演示用量可忽略；每次调用记录 token 数供核查

### 6A.4 风险与对策

| 风险 | 对策 |
|---|---|
| 答辩机无外网 | 历史结果回放 + 演示前预生成；降级提示兜底 |
| API 费用 | DeepSeek 极低；max_tokens/上下文截断控制；token 记录可查 |
| 生成内容不可控/幻觉 | temperature 0.3 + JSON 结构约束 + 内容仅展示不执行；论文如实讨论局限 |
| key 泄露 | 服务端存储 + 环境变量；前端不渲染 key |

### 6A.5 AI 质量评估实验（补强：把"调 API"变成有数据支撑的研究）

**目的**：量化 AI 研判输出的可用性，产出论文实验数据，避免被质疑"只是调接口"。

**方法**：
1. 从告警库分层抽样 N=30 条（覆盖高/中/低级别与正则/聚合/复合各规则类型）
2. `eval.py` 批量调用研判 API → 导出 `docs/experiments/ai_eval.csv`（告警信息 / 规则级别 / 规则类型 / AI 输出级别 / AI 文本 / 状态）
3. 两个评估维度：
   - **一致性**：AI 输出级别 vs 规则定义级别，自动统计一致率
   - **可用性**：AI 文本按 5 分制人工评分（可请同学盲评），统计平均分与可用率（≥3 分占比）
4. Prompt 迭代 ≥2 轮，对比改进前后的一致率/可用率——"改进过程"本身就是论文实验内容
5. 结果整理进 `docs/experiments/ai_eval.md`（含成功样例 5 条 + 失败/降级样例，如实记录幻觉/格式错误案例）

**论文口径**：如实报告一致率、可用率、失败率与典型失败案例，并讨论局限与改进方向。

---

## 6B. 学术实验体系（补强：独立于系统的实验脚本）

> 设计决策 6"实验与系统分离"落地：所有学术实验以独立脚本运行在系统之外，产出指标文档供论文第 6 章引用。

| 脚本（tests/experiments/） | 实验内容 | 产出（docs/experiments/） | 里程碑 |
|---|---|---|---|
| run_dataset_eval.py | ML 双场景评估：模拟器数据 vs NSL-KDD / CICIDS2017 | dataset_eval.md（AUC/ROC/PR 对比表 + 混淆矩阵） | M3 后 |
| run_fp_eval.py | 误报率长跑：正常流量剧本 1 万条统计误告警 | fp_eval.md（误报率 + 误报样本分析） | M5 后 |
| run_ai_eval.py | AI 研判质量评估（6A.5 的自动化部分） | ai_eval.csv / ai_eval.md | AI-2 |
| run_perf_eval.py | 性能基准：入库吞吐、查询延迟、扫描耗时 | perf.md | M6 |

**数据集说明**：NSL-KDD（网络入侵检测经典基准，ARFF/CSV 约 12 万条）与 CICIDS2017（加拿大 CIC 数据集，含 DoS/扫描/爆破等现代攻击，CSV 需下载解压约 50GB 全量、可用按日采样子集）均需从公开渠道下载到 `data/datasets/`（论文附录注明出处与下载日期）。

---

## 7. 里程碑总览（6 个 + AI 阶段，3~6 个月）

| 阶段 | 时间 | 功能清单 | 验收标准 |
|---|---|---|---|
| M1 骨架+日志链路 | 第 1 月 | 骨架、SQLite、登录、日志解析器、模拟器、日志导入与查询页 | 快速演示 rate=50/duration=10（10 秒 ≥500 条）；日志页可筛选回放；登录锁定生效；parser/simulator 测试通过 |
| M2 规则引擎+告警 | 第 2 月 | 规则引擎、15 条内置规则、规则管理页、告警服务、告警页、仪表盘 v1 | SSH 爆破剧本 5 分钟内自动告警且 count 累加；规则测试工具可用 |
| M3 机器学习+数据集验证 | 2.5~3.5 月 | 特征提取、孤立森林+K-means、ML 页、ML 告警接入、dataset_adapter.py | 出 ROC/PR 数值；阈值滑块实时生效；Top3 特征解释；**双场景评估报告产出（模拟 vs NSL-KDD）** |
| M4 扫描器骨架+前两个检测器 | 3.5~4.5 月 | 目标/任务调度、HTTP 客户端、信息收集、SQLi+XSS 检测器（含绕过变体）、本地靶场 | 靶场预置漏洞 100% 检出；**绕过变体用例测试通过** |
| M5 完整扫描器+闭环整合 | 4.5~5.5 月 | 剩余 4 检测器（含绕过变体）、报告生成、patterns.py 统一、扫描与告警关联、仪表盘补全 | **核心答辩演示**：扫描发现漏洞→模拟器生成攻击→日志告警关联到该发现；一键出报告 |
| M6 收尾+论文 | 5.5~6 月 | 全量测试、README/部署文档、演示脚本+截图、perf_eval 性能基准、论文初稿、答辩演练 | 冷启动到完整演示 ≤10 分钟脚本化走通；全部实验文档齐备 |
| AI-1 告警研判 | M2 后 ~2 周 | ai/client.py、prompts.py、ai_insights 表、告警详情页 AI 研判卡片、系统设置页 | 有 key 时告警一键出研判（危害/根因/处置）；无 key 时提示不可用且系统正常 |
| AI-2 报告+日报+质量评估 | M5 后 ~3 周 | report_writer.py、daily_report.py、报告页 AI 报告/日报、eval.py 质量评估实验 | 一键生成自然语言报告/日报；**ai_eval 报告产出（一致率/可用率/Prompt 迭代对比）** |

**误报率长跑实验**并入 M5 后（1 万条正常流量，产出 fp_eval.md）。

**裁剪预案**（时间不够时）：ML 可删 K-means；扫描器可只留 3 个检测器；IP 地图可用简化散点；数据集验证可只做 NSL-KDD 一个；**AI 研判为论文重点功能，尽量保留**，AI 日报与质量评估可裁。核心闭环（规则告警 + 一个扫描器 + AI 研判）不可裁。

---

## 8. 论文大纲要点

第1章 绪论（含题目创新点"主被动结合"与系统亮点"智能研判"、学术可信度设计：外部数据集验证 + AI 质量评估）→ 第2章 相关技术（SIEM、规则引擎、孤立森林/K-means、SQLi/XSS 原理、**大语言模型与 DeepSeek API**、**NSL-KDD/CICIDS 数据集综述**、技术选型）→ 第3章 需求分析 → 第4章 总体设计（分层架构、双链路设计、**主被动结合设计**、**AI 智能研判设计**、三层协同架构、数据库 ER）→ 第5章 详细设计与实现（含**基于大模型的智能研判模块**：client/prompts/研判与报告服务/降级与回放、ML 数据集适配器、检测器对抗性变体）→ 第6章 测试与实验分析（功能测试、规则命中率与**误报率长跑实验**、**ML 双场景对比实验（模拟 vs NSL-KDD/CICIDS）**、扫描器检出与**绕过变体测试**、**AI 输出质量评估实验（一致率/可用率/Prompt 迭代）**、性能基准）→ 第7章 总结展望（**规则抓已知、ML 抓未知、LLM 做研判解释的三层协同**）。附录：内置规则清单、修复建议明细、建表脚本、数据集说明。

---

## 附录 A：项目目录结构

```
security-platform/
├── 毕设项目方案.md  需求方案.md  验收测试方案.md
├── app.py  config.py  requirements.txt  README.md
├── secplat/
│   ├── __init__.py  models.py
│   ├── blueprints/  auth dashboard logs alerts rules ml scanner report settings
│   ├── engine/      log_parser log_simulator rule_engine alert_service
│   │   ├── patterns.py  rules/builtin_rules.py
│   │   └── ml/  features.py  isolation_forest.py  kmeans.py  dataset_adapter.py
│   ├── scanner/     core.py  http_client.py  report.py
│   │   ├── info_gather/  cms_fingerprint headers robots dir_brute crawler
│   │   └── detectors/    sqli xss sensitive_file path_traversal security_headers cve_match
│   ├── ai/          client.py  prompts.py  alert_review.py  report_writer.py  daily_report.py  eval.py
│   ├── utils/       timewin  ipgeo  batch  live_feed
│   ├── templates/   base login dashboard logs logs_sources + M2~M5 页面 + settings
│   └── static/      css/  js/  bootstrap/echarts 本地引入
├── data/            app.db  raw_logs/  reports/  models/  datasets/(NSL-KDD/CICIDS)
├── tests/           每模块独立测试 + smoke.sh 一键冒烟
│   └── experiments/ run_dataset_eval  run_fp_eval  run_ai_eval  run_perf_eval
├── target_lab/      带漏洞的 Flask 靶场小站（M4）
└── docs/            论文素材/截图/演示脚本/测试记录/experiments/(实验报告产出)
```

## 附录 B：15 条内置检测规则明细（M2 实施直接照抄，论文附录复用）

| # | 规则名称 | 类型 | 检测要点 | 级别 |
|---|---|---|---|---|
| 1 | SSH 暴力破解 | 聚合 | src_ip 在 300s 内失败登录 ≥5 次 | 高 |
| 2 | SSH 用户名枚举 | 聚合 | 60s 内 ≥3 次不常见用户名失败 | 中 |
| 3 | 端口扫描（端口多样性） | 聚合 | src_ip 60s 内去重目标端口 ≥20 | 高 |
| 4 | 端口扫描（异常 TCP 标志） | 正则+计数 | FIN/URG/PSH 无 SYN（Nmap -sF/-sN），30s ≥10 | 高 |
| 5 | 扫描器 UA 指纹 | 正则 | UA 匹配 sqlmap/nikto/nessus/awvs/nmap/masscan/wpscan | 中 |
| 6 | SQL 注入特征 | 正则 | URL 匹配 union select/sleep(/or 1=1/-- /%27%20or | 高 |
| 7 | XSS 特征 | 正则 | 参数匹配 <script/javascript:/onerror=/onload=/alert( | 高 |
| 8 | 目录遍历 | 正则 | 参数匹配 ../、..%2f、%2e%2e | 高 |
| 9 | 命令注入 | 正则 | 匹配 ;id/;whoami/$()/管道符变体 | 高 |
| 10 | 敏感路径访问 | 正则 | URL 匹配 /.git/config、.env、web.config、.bak、.sql | 中 |
| 11 | 高频错误响应 | 聚合 | src_ip 60s 内状态码 ≥400 达 30 次 | 中 |
| 12 | 请求速率异常 | 聚合 | src_ip 60s 内请求 ≥200 | 低 |
| 13 | 可疑 UA（空/极短） | 正则 | UA 长度 <5 且 method=POST | 低 |
| 14 | 双重编码攻击 | 正则 | 参数匹配 %25/%2527/%252e | 中 |
| 15 | 爆破成功后登录 | 复合 | 同一 src_ip 60s 内 ≥3 次失败后出现 1 次成功 | 高 |

## 变更记录

| 日期 | 版本 | 变更内容 |
|---|---|---|
| 2026-09-01 | v1.0 | 初始完整设计（宏观蓝图 + M1 详细设计） |
| 2026-09-01 | v1.1 | 新增 AI 集成设计（DeepSeek 告警研判/报告/日报）、系统设置页（9→10 页面）、ai_insights 表（13→14 表）、登录安全增强（session 过期/失败锁定）、日志膨胀控制、M1 验收更新（快速演示参数/锁定测试）；文档头加"相关文档"节；新增附录 B 规则明细；论文大纲加大模型内容 |
| 2026-09-08 | v1.2 | 毕设深度补强：① ML 增加公开数据集验证（dataset_adapter.py + NSL-KDD/CICIDS 双场景实验）；② 检测器增加对抗性绕过变体用例；③ AI 增加质量评估实验（eval.py，一致率/可用率/Prompt 迭代，6A.5）；④ 新增 6B 学术实验体系（误报率长跑/性能基准）；⑤ 里程碑时间轴相应调整（M3/M5/AI-2 扩充至 6 个月）；⑥ 论文大纲加实验章节与数据集综述 |
| 2026-09-12 | v1.3 | **题目定稿《基于主被动结合的中小企业网络安全检测平台》**；应用场景明确为中小企业安全防护；AI 智能研判定位调整为"系统重要亮点（不占题目，论文重点章节）"；裁剪预案与创新点表述同步更新 |
| 2026-09-15 | v1.4 | 应用场景补充用户画像（20~200 人 / 1~10 台服务器 / 无专职安全人员 / 预算受限），与《需求方案.md》1.1 节互引，明确场景边界 |
